spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  application:
    name: orchestrator-service

  liquibase:
    enabled: false
    change-log: classpath:/db/changelog.xml

server:
  port: 8080